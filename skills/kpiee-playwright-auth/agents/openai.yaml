interface:
  display_name: "KPIEE Playwright Auth"
  short_description: "dx-kpiee の Playwright 認証を human login + state 保存で安定化する"
  default_prompt: "Use $kpiee-playwright-auth when you need authenticated Playwright access for dx-kpiee investigations or small automations and want to avoid storing credentials, have a human log in once, save storageState, and reuse environment-separated auth files safely."
