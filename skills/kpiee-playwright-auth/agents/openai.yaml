interface:
  display_name: "KPIEE Playwright Auth"
  short_description: "kpiee の playwright-cli 認証を human login + state-save/load で安定化する"
  default_prompt: "Use $kpiee-playwright-auth when you need authenticated playwright-cli access for kpiee investigations or small automations. It uses human login + state-save/state-load to reuse environment-separated storageState files without storing credentials."
